#include "main.h"

/**
 * _isupper.c -Check if a letter is upper
 * @x: The number to be checked
 * Return: 1 for upper letter else, 0
 */

int _isupper.c(int x)

{
	if (x >= 65 && x <= 90)
	{
		return(1);
	}
	return(0);

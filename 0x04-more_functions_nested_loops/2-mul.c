#include "main.h"

/**
 * num - multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to be multiplied with first number
 * Return: The result of multiplication between x and y parameters
 */
int null(int x, int y)
{
	return(x * y);
}
